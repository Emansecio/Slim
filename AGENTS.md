Conclua com a menor solução correta, verificável e sustentável. Entenda o pedido antes de agir; análise não autoriza alteração. RULES.md é o protocolo técnico de evidência e deploy; não transforme exemplos ou histórico em obrigação.
Tarefas simples: informe e execute. Risco ou vários passos: plano curto com escopo e critérios. Pergunte só decisão material; explicite suposições e prossiga nas rotinas.
Pedido de implementação cobre correção, execução e validação até os critérios; não peça nova autorização para etapas já abrangidas.
Comece no fluxo afetado. Corrija a causa demonstrada e a consistência necessária; preserve contratos; não limpe, renomeie nem cubra hipóteses.
Prefira padrões e infraestrutura existentes. Entregue comportamento funcional, sem stubs/TODOs; trate falhas previsíveis sem fallback silencioso.
Um agente por padrão; delegue só partes independentes. Use skills/ferramentas pertinentes; não instale nada nem altere config global; não crie relatórios, backups ou infra auxiliar sem pedido. No PowerShell, descarte saída com `| Out-Null`, `$null = ...` ou `> $null`; nunca `> nul`/`2> nul`.
Antes de editar, confira status/diff. Preserve trabalho alheio; conflito direto exige esclarecimento. Sem commit, push ou alteração fora do escopo sem pedido explícito; alterar código do Slim inclui publicar o binário no PATH ao final. Não altere funções de restrição, segurança ou delimitação sem avisar antes.
Git, testes, troca de branch e restore não são automaticamente seguros; não use reset --hard, git clean ou restaurações destrutivas. Ao desfazer, reverta só o que você introduziu.
Valide o comportamento modificado conforme RULES.md, com a menor checagem suficiente. `.\refresh-slim.ps1` publica o binário no PATH e encerra o fluxo normal de edição do Slim (`-Test` se a suíte completa for necessária). Não enfraqueça testes. Corrija regressões causadas; relate falhas preexistentes sem absorvê-las.
Revise o diff e encerre ao atender os critérios. Resuma o que mudou, o que foi verificado e o que não foi; use o checklist de RULES.md §4 e marque o não aplicável. Não prometa o que não mediu.
