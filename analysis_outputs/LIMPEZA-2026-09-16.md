# Limpeza de APIs e infraestrutura isolada — 16/09/2026

## Resultado

Limpeza implementada no checkout baseado em `03344ad`, sem commit, push ou deploy.
Foram removidas **2.571 linhas Rust líquidas (1,60%)**: 2.604 linhas excluídas
e 33 adicionadas na substituição do smoke, ajustes de imports e testes restantes.
Foram excluídos 11 arquivos Rust rastreados e dois artefatos locais não rastreados.

| Conjunto | Antes | Depois | Redução líquida | Redução |
|---|---:|---:|---:|---:|
| Rust em `crates/*/src` | 108.207 | 106.015 | 2.192 | 2,03% |
| Demais arquivos Rust rastreados | 52.760 | 52.381 | 379 | 0,72% |
| **Todo Rust rastreado** | **160.967** | **158.396** | **2.571** | **1,60%** |

Método: linhas físicas, incluindo comentários, espaços e testes internos; os
arquivos `src` não são uma contagem exclusiva de código executável. A base foi
capturada antes desta implementação. A porcentagem é `(antes - depois) / antes`.
As alterações preexistentes de layout foram descontadas. Markdown, lockfiles,
arquivos gerados, `target` e os dois artefatos locais não entram na conta Rust.
Não foi medido ganho de desempenho, memória ou tamanho de binário.

## 1. APIs sem consumidores internos

Removidos os 24 candidatos do inventário de core/LSP:

| Área | Símbolos |
|---|---|
| Runtime | `set_artifact_store`, `run_agent_loop_with_message` |
| Process/tools | `resolve_configured`, `with_process_runner` |
| Contexto | `threshold_tokens`, `mark_ready` |
| Model/sessões | `clear_event_sender`, `is_healthy`, `operation_was_started`, `requeue_suspended`, `restore_repo` |
| Tool phases | `reconstruct_batches`, `incomplete_calls`, `incomplete_batches`, `replayable` |
| Provider | `with_shared_transport_and_cache`, `stream_compaction_messages_cancellable` |
| MCP | `McpServerStatus::tool_count` |
| LSP | `PROTOCOL_VERSION`, `close_document`, `document_snapshot_async`, `diagnostics_for`, `lsp_document_identifier`, `utf16_units_of` |

Também saíram o wrapper `run_http_provider` e o helper privado
`_is_skill_directory`. Permanecem as implementações canônicas, os métodos de
traits, o fechamento LSP por eviction e as APIs de cache exercitadas por testes.
`run_http_provider_messages` continua disponível e testado.

## 2. Observabilidade isolada e scheduler legado

Removidos os quatro módulos `session/observability_*` e seus reexports/testes
exclusivos. Esses arquivos não participavam do fluxo produtivo CLI/TUI.
O journal produtivo, os fatos de ferramentas, os helpers de arquivo usados pelo
`JsonlRepo`, o reducer, os repositórios e o schema durável foram preservados.

Removidos `agents::Scheduler`, `MutationLease`, `Activity`, `SpawnRequest`,
`SpawnResult` e `ChildResult`. O tipo `ChildStatus` e o lifecycle durável de
child em `CapabilityService` continuam disponíveis.

O teste de branch mantém verificação de identidade, cutoff e índice; o E2E
mantém os cenários restantes de runtime/TUI. Apenas os trechos exclusivamente
ligados às implementações removidas foram retirados.

## 3. Smoke e TUI

- Removidos `compose_app`, `render_snapshot` e o snapshot artificial de
  `AppHandle`, incluindo armazenamento, setter, getter e reexport.
- Removido também o antigo `session::SessionSnapshot`. O evento real
  `UiEvent::SessionSnapshot` permanece integrado.
- O smoke agora percorre `AppHandle → UiEvent::from_core → AppState →
  render_terminal_text` e verifica o texto realmente renderizado.
- Removidos `block_search_text`, `search_match_indices`, `safe_block_text`,
  `is_interaction_protocol`, `run_terminal_id`, `invalidate_render_configuration`,
  os dois formatters de tokens/s, `render_sparkline`, `render_fractional_bar`,
  `format_context_bar` e `status_line`, junto dos testes exclusivos desses helpers.
- Preservados busca filtrada, renderização real, invalidações em uso e
  `WakeSignal::raw_handle`; retirada somente sua supressão obsoleta.
- O teste golden mantém assertions sobre o footer real; saiu apenas a assertion
  redundante sobre `status_line`.

## 4. Arquivos excluídos

| Grupo | Arquivos |
|---|---|
| Observabilidade | `crates/slim-core/src/session/observability_hooks.rs`, `observability_snapshot.rs`, `observability_telemetry.rs`, `observability_watch.rs` |
| Snapshot legado | `crates/slim-core/src/session/snapshot.rs` |
| Scheduler legado | `crates/slim-core/src/agents/activity.rs`, `scheduler.rs` |
| Testes exclusivos | `crates/slim-core/tests/session_observability.rs`, `subagent_contract.rs` |
| Fixtures artificiais | `tests/support/fake_clock.rs`, `tests/support/fake_provider.rs` |
| Artefatos locais | `nul` (90 bytes), `bench/luna-live/AUDIT-2026-09-15.zip` (475.451 bytes) |

Os diretórios `bench`, `poc` e `analysis_outputs` foram preservados. Os dois
artefatos locais somavam 475.541 bytes e estavam ausentes na conferência final.
`AGENTS.md` orienta usar `$null`/`Out-Null` no PowerShell, evitando `> nul`.
Não foi identificado um gerador específico de `nul` nos scripts pesquisados;
a orientação não impede uma ferramenta externa de recriá-lo.

## 5. Compatibilidade e preservação

Plan/Goal e as variantes de `TaskMutation` foram mantidos para preservar a
leitura dos registros `task.v1` já aceitos. A geração produtiva de TODO, sua
persistência e retomada continuam no mesmo caminho. Não houve alteração de schema.

A remoção de APIs públicas é incompatível com eventuais consumidores externos
dessas APIs. Não foi introduzida uma camada de aliases ou wrappers de compatibilidade.
A redução de superfície foi feita por remoção dos candidatos verificados; não
houve redução indiscriminada de visibilidade de APIs usadas pelo POC ou testes.

Foram preservados por comparação com a base inicial os arquivos preexistentes
`DESIGN-SLIM-TUI.md`, `slim-tui/src/runtime.rs`, `inspectors_golden.rs` e
`release/README.md`. Em `layout_golden.rs`, o trabalho de largura foi preservado;
esta tarefa removeu somente a assertion sobre o wrapper extinto.

README e índice/tracker do harness registram a retirada das APIs sem apagar os
gates históricos. Três workers Luna implementaram escopos separados; revisão
independente Astra e integração pelo root não deixaram findings materiais da
limpeza pendentes. Um reexport transitório inválido foi corrigido antes dos gates.

## 6. Validação executada

| Verificação | Resultado |
|---|---|
| `cargo fmt --all -- --check` | Aprovado após dois ajustes de formatação |
| `git diff --check` | Aprovado; avisos de conversão CRLF, sem erros de whitespace |
| `cargo clippy --workspace --lib --bins --offline -- -D warnings` | Aprovado, exit 0 |
| `cargo test --workspace --offline` | **1.654 passed, 0 failed, 34 ignored; exit 0** |
| Doc-tests | Incluídos na execução; quatro targets, zero casos |
| Smoke real e retomada TODO | Ambos aprovados na suíte completa |
| `cargo clippy --workspace --all-targets --offline -- -D warnings` | Não aprovado: lint preexistente no teste de largura |
| `cargo check --manifest-path poc/tui/Cargo.toml --offline` | Não aprovado: incompatibilidades preexistentes do POC |

Foram contabilizados 83 resultados de suites/targets no log da execução completa.
Os 34 testes ignorados não foram tratados como aprovados nem executados à força.
Toolchain verificado: cargo/rustc/rustdoc 1.98.0. Log da suíte:
`C:\Users\User\AppData\Local\Temp\slim-cleanup-20260916-tests.log`.

O lint preexistente é `needless_borrows_for_generic_args` no novo teste de
largura em `layout_golden.rs`: `insert_text(&"x".repeat(150))`. A linha foi
confirmada na base anterior a esta tarefa e preservada.

O POC tem quatro chamadas `FullscreenBackend::start()` sem o argumento
`Capabilities` exigido e acesso direto ao campo privado `AppState::blocks`.
Os arquivos responsáveis estavam inalterados antes e depois da limpeza.
A tentativa de check atualizou seu lockfile; somente essa atualização gerada
foi desfeita, restaurando os bytes iniciais previamente verificados por SHA-256.
Não foi alegada validação bem-sucedida do POC com seu lockfile original.

## 7. Certificação de integração

### [ESTADO: Integração de 3 itens]

- **Modo:** INTEGRATION_REVIEW.
- **Certificações locais:** `cleanup_apis`, `cleanup_isolated` e
  `cleanup_tui_smoke`, recebidas; validação integrada executada pelo root.
- **Conformidade de escopo:** APIs sem consumidores, infraestrutura isolada e
  smoke/helpers TUI; documentação e artefatos locais tratados pelo root.
- **Fronteiras verificadas:** reexports entre crates, testes, consumers do POC,
  `ChildStatus`, journal e schema `task.v1`.
- **Trecho do diff agregado:**

  ```diff
  -pub use model::{
  -    AppHandle, EventQueueStats, SessionEventReceiver, SessionEventSender, SessionSnapshot,
  -};
  +pub use model::{AppHandle, EventQueueStats, SessionEventReceiver, SessionEventSender};
  -pub use child_session::{ChildResult, ChildStatus, SpawnRequest, SpawnResult};
  -pub use scheduler::{MutationLease, Scheduler};
  +pub use child_session::ChildStatus;
  ```

- **Análise de integração:** reexport transitório inválido corrigido; WIP
  preexistente preservado; nenhuma mudança no formato durável.
- **Verificação:** suíte completa e clippy de produção aprovados; limitações do
  clippy all-targets e POC descritas na seção 6, sem ocultar suas falhas.
- **Itens reabertos:** nenhum pendente.
- **Certificação:** **[CERTIFICADO DE INTEGRAÇÃO]**.

## 8. Entrega

**Integração concluída**, com as limitações preexistentes acima identificadas.
Sem commit, push ou deploy. As mudanças estão apenas no checkout; o executável
instalado não foi atualizado. Não houve validação live com provider pago nem
validação manual em console físico.
